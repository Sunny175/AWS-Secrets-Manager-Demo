spring:
  application:
    name: AWS-Secrets-Manager-Demo
  config:
    import: "optional:aws-secretsmanager:/AWS-Secrets-Manager-Demo/third-party-token"

cloud:
  aws:
    region:
      static: us-east-1

watchmode:
  api-key: ${WATCHMODE_API_KEY}
  base-url: https://api.watchmode.com/v1


